import styled from "styled-components"

export const Link = styled.span`
    font-size: 12px;
    margin: 0;
    padding: 0;

    color: #0000FF;
    cursor: pointer;

    &:hover {
        color: #772233;
    }

    &:active {
        color: #FF0000;
    }

    &:visited {
        color: #0B0080;
    }
`;